import React from 'react';
import { Link } from "react-router-dom";

export default function(props) {
    return (
        <div>
            <h2>errorcito! </h2>
            <Link to="/">llévame a la página principal, porfi</Link>
        </div>
    );
}